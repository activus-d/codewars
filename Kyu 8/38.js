/*function isDivideBy(number, a, b) {
  good luck
  return (Math.abs(number) / a) % 1 === 0 && (Math.abs(number) / b) % 1 === 0 ? true : false} */
function isDivideBy(number, a, b) {
    // good luck
    return (Math.abs(number) / a) % 1 === 0 && (Math.abs(number) / b) % 1 === 0 ? true : false
}